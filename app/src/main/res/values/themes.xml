<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.ToDoQuest" parent="Base.Theme.ToDoQuest" />
    <!-- Base application theme. -->
    <style name="Base.Theme.ToDoQuest" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="android:windowBackground">@drawable/background_gradient_main</item>
        <item name="android:statusBarColor">#00D65B26</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>+
    </style>
</resources>